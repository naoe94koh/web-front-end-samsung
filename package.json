{
  "scripts": {
    "dev": "npm run sass | npm run serve",
    "sass": "sass --watch --update src/sass:public/styles",
    "serve": "live-server public --host=localhost --port=3000 --no-browser"
  },
  "devDependencies": {
    "live-server": "1.2.2",
    "sass": "1.54.9"
  }
}
