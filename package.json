{
  "scripts": {
    "dev": "live-server public --host=localhost --port=3000 --no-browser",
    "compile": "sass src/sass:public/assets/styles"
  },
  "devDependencies": {
    "live-server": "1.2.2",
    "sass": "1.54.9",
    "tailwindcss": "3.1.8"
  }
}