{
  "scripts": {
    "dev": "run-p watch serve",
    "serve": "live-server public --host=localhost --port=3000 --no-browser",
    "compile": "sass src/sass:public/styles",
    "watch": "npm run compile -- --watch --update",
    "build": "npm run compile -- --style=compressed --no-source-map"
  },
  "devDependencies": {
    "live-server": "1.2.2",
    "npm-run-all": "4.1.5",
    "sass": "1.54.9"
  }
}
